# Risks and Technical Debt

Audit date: 2026-07-17. “Blocks” means unsafe or impossible to build the named future capability without first resolving the finding.

> **⚠️ SUPERSEDED (2026-07-20) — pre-build snapshot.** This register predates Stages 0–7
> and was not refreshed. Most rows below are resolved: **R-01** (tenant root → Organization,
> Stage 1 / B5), **R-02** (CI exists; fonts partly self-hosted), **R-03** (isolated test
> harness, S0-003), **R-04** (migrations reconciled, S0-004), **R-06** (publishing shipped,
> Stage 2). Still open: **R-05** — RLS is built but **inert** (no non-BYPASSRLS role yet;
> see [`RLS_ROLLOUT_AND_OPS.md`](./RLS_ROLLOUT_AND_OPS.md)); **R-10** (CSRF missing-origin,
> mitigated in B3); **R-13** (credential rotation). New risks from the 2026-07-20 audit —
> no Nest bootstrap/DI test, no CD pipeline, no observability, placeholder frontends
> shipping — are filed as GitHub issues **#90, #98–#108**. **Source of truth for status:
> [`IMPLEMENTATION_BACKLOG.md`](./IMPLEMENTATION_BACKLOG.md).**

| ID   | Severity | Finding and evidence                                                                                                                                                                                                                                                       | Impact                                                                                                                                                         | Recommended resolution                                                                                                                                                                              | Dependencies                    | Blocks                               |
| ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ------------------------------------ |
| R-01 | Critical | Tenant root is ambiguous: `Workspace` is called an enterprise/Y2 feature and `Store.workspaceId` is nullable (`packages/database/prisma/schema.prisma`). Authorization operates on `StoreOwner`/`StoreMembers` (`apps/api.saroh.in/src/modules/stores/stores.service.ts`). | CRM, bookings, subscriptions and multiple sites can attach to different roots; access rules become inconsistent and data migration becomes increasingly risky. | Implement the accepted Organization tenant: Organization owns all business data; Projects are optional groupings; Sites and Stores are Organization-owned modules. Migrate incrementally.           | DEC-005 accepted                | Stage 1 and all new business modules |
| R-02 | High     | No CI workflow exists; root lint is broken (`next lint` in every Next app) and API lint is an echo (`apps/api.saroh.in/package.json`). Root build is not reproducible offline because `apps/saroh.in` fetches Google Fonts.                                                | Broken code can merge; release confidence is low.                                                                                                              | Add deterministic lint/typecheck/test/build tasks and required CI gates; self-host fonts or make builds network-independent.                                                                        | R-03                            | Stage 1                              |
| R-03 | High     | Five DB-backed API suites use the developer `DATABASE_URL` and fail against schema drift; there is no isolated test database lifecycle (`apps/api.saroh.in/src/modules/**/*.spec.ts`, `packages/database/prisma`).                                                         | Tests can mutate shared data and cannot reliably validate tenant isolation or order correctness.                                                               | Provision disposable PostgreSQL per test run, apply migrations, enforce a test URL guard, seed fixtures, and clean safely.                                                                          | CI                              | Stage 1, commerce changes            |
| R-04 | High     | Only one initial migration exists while the active schema and configured database are demonstrably out of sync (`packages/database/prisma/migrations`, API test failures). A timestamped schema backup is committed.                                                       | Production deploys may fail or silently diverge; rollback/audit history is unclear.                                                                            | Establish migration-baseline ownership, diff deployed schemas, create forward-only reconciliation migrations, and remove backup after verification.                                                 | Database inventory              | All schema changes                   |
| R-05 | High     | Cross-tenant consistency is enforced mainly in service code. Child foreign keys do not encode tenant identity, stores may be workspace-less, and no RLS/tenant Prisma guard exists.                                                                                        | A missed predicate or cross-store ID can expose/corrupt tenant data.                                                                                           | Require Organization-scoped repositories, compound ownership constraints and adversarial isolation tests; implement transaction-local Organization context and the accepted PostgreSQL RLS defense. | R-01, R-03, DEC-007             | CRM, bookings, commerce              |
| R-06 | High     | Public site renderer is a deliberate null-data stub (`apps/sites.saroh.in/lib/fetchers.ts`); schema lacks Site/Page/Section/TemplateVersion.                                                                                                                               | Core promise “build and publish websites” is absent; current renderer always 404s.                                                                             | Deliver one Site → Page → Version → Publication API/read model and connect renderer before broadening features.                                                                                     | R-01                            | First target journey                 |
| R-07 | High     | Marketing waitlist logs submitted email and returns success without persistence or validation (`apps/saroh.in/app/api/waitlist/route.ts`).                                                                                                                                 | Users see false success; PII can enter logs; leads are lost.                                                                                                   | Disable/label the form or implement validated, rate-limited API persistence with consent and notification.                                                                                          | Forms/contacts model            | Lead journey                         |
| R-08 | Medium   | Auth migration documentation is contradictory: README says `app` still uses NextAuth, docs teach NextAuth, while source uses Better Auth; comments disagree about whether accounts or API hosts auth.                                                                      | Operators configure wrong callbacks/secrets; developers may reintroduce duplicate auth.                                                                        | Declare API as auth host, accounts as UI, delete NextAuth docs/references after repository-wide verification, add an auth architecture test/runbook.                                                | Product domain decision         | No, but raises security risk         |
| R-09 | Medium   | RBAC roles/statuses are free-form strings and policy checks are scattered (`schema.prisma`, `stores.service.ts`, member/content/product services).                                                                                                                         | Invalid roles and policy drift; difficult auditing and future custom roles.                                                                                    | Centralize typed permissions and policy evaluation at API application boundary; migrate values to enums/permission grants incrementally.                                                            | R-01                            | Advanced collaboration               |
| R-10 | Medium   | Auth CSRF checks reject an invalid origin only when header exists (`better-auth.guard.ts`, `packages/auth/src/middleware.ts`); cross-subdomain cookie behavior lacks browser/E2E coverage.                                                                                 | Proxy/client edge cases could bypass assumed defense or break login in production.                                                                             | Document Better Auth’s token protections, reject missing origin where browser mutation semantics permit, and add real cross-origin cookie/CSRF tests.                                               | Test environment/domain harness | Production auth hardening            |
| R-11 | Medium   | No background job abstraction, idempotent webhook framework, outbox or provider interfaces exist. Email is direct SMTP; payment/storage settings are schema/env concepts.                                                                                                  | Notifications, payments and integrations will be unreliable and tightly coupled.                                                                               | Implement the accepted PostgreSQL transactional outbox and PostgreSQL-backed job runner behind `JobQueue`; retain BullMQ/Redis as the measured scaling path.                                        | R-01, DEC-008                   | Payments and communications          |
| R-12 | Medium   | Environment use exceeds documentation (`API_URL`, `APP_URL`, `NEXT_PUBLIC_AUTH_APP_URL`, `SMTP_HOSTNAME`, `USER_ACCOUNT`, etc.); Turbo tracks only three global env vars.                                                                                                  | Builds/config differ silently and secrets may be requested by the wrong app.                                                                                   | Add typed per-app env schemas, ownership tables, startup validation and complete Turbo env declarations.                                                                                            | Canonical deployment map        | CI/deployment                        |
| R-13 | Medium   | No secret-like tracked `.env` file was found, but local real env files exist. An untracked (never committed) `.github/# Code Citations.md` containing pasted third-party architecture/code material has since been removed.                                                | Local secrets remain operational risk; the provenance concern from the copied material is resolved now that the artifact is gone.                              | Keep secret scanning in CI, rotate any historically shared credential; the citation artifact has been removed.                                                                                      | Repository owner review         | Public release                       |
| R-14 | Medium   | Domain/naming is inconsistent: root/package metadata and remote use `saroh.io`; deployed app names and target product use `saroh.in`; `.github` points to multiple org/account names.                                                                                      | OAuth/cookies/SEO/support links and package provenance can diverge.                                                                                            | Choose `saroh.in` as canonical product/runtime domain, reserve/redirect `.io`, and normalize repository/package metadata separately.                                                                | Product-owner decision          | Deployment hardening                 |
| R-15 | Medium   | No error taxonomy, request IDs, structured logger, metrics, tracing, audit-log service or alerting is wired. Some frontend adapters convert any read error to empty state.                                                                                                 | Incidents and authorization failures are difficult to distinguish; UI can hide outages as “no data.”                                                           | Add API problem details, correlation IDs and structured logs; preserve typed error states through frontend adapters; define audit events.                                                           | Observability choice            | Production readiness                 |
| R-16 | Medium   | UI/API contract types are duplicated manually in `apps/app.saroh.in/lib/*/service.ts`; no OpenAPI generation/SDK or contract tests exist.                                                                                                                                  | Schema drift causes runtime failures despite typechecks.                                                                                                       | Publish OpenAPI from Nest, generate or validate a thin typed client, and add contract tests.                                                                                                        | API error model                 | Faster module work                   |
| R-17 | Low      | `@saroh/templates` is empty with a failing test; template app/admin are scaffolds; copied utilities exist in app/sites and UI code exists both locally and shared.                                                                                                         | Navigation and maintenance noise; unclear ownership.                                                                                                           | Classify each area as active/scaffold/deprecated, then consolidate only after call-site and deployment verification.                                                                                | Product roadmap                 | No                                   |
| R-18 | Low      | Next/React versions vary between catalogs and direct pins; Node engines exist only for API and local command execution used newer Node versions.                                                                                                                           | Non-reproducible builds and subtle framework behavior differences.                                                                                             | Add root `engines`/toolchain file, align catalog versions, and test supported Node LTS in CI.                                                                                                       | CI                              | No                                   |

## Top five immediate risks

1. R-01 ambiguous tenant root.
2. R-05 service-only tenant isolation.
3. R-03 non-hermetic failing integration tests.
4. R-02 absent/broken quality gates.
5. R-06 no end-to-end website publishing path.
