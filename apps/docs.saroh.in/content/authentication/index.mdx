---
title: "Authenticating with Better Auth"
---

## Authentication in Saroh.io

Saroh.io uses [Better Auth](https://www.better-auth.com/) as its single identity
system. The auth server is hosted by the API (`api.saroh.in`), while
`accounts.saroh.in` provides the sign-in / signup UI.
